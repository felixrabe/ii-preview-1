import actions from '../actions/actions'

const defaultState = {}

const editors = (state = defaultState, action) => {
  switch (action.type) {
  default:
    return state
  }
}

export default editors
