import actions from '../actions/actions'

const defaultState = null

const modifiedEditor = (state = defaultState, action) => {
  switch (action.type) {
  default:
    return state
  }
}

export default modifiedEditor
