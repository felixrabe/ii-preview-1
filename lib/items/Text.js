import React from 'react'
import {connect} from 'react-redux'

const Text = ({}) => (
  <div className='ii-text'>
    Text
  </div>
)

export default connect(
  state => ({

  }),
  dispatch => ({

  }),
)(Text)
