{
  "name": "ii-1",
  "version": "0.1.0",
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "postinstall": "[[ -e ./node_modules/.bin/ii-1 ]] || (echo again... ; yarn --force)"
  },
  "workspaces": [
    "workspaces/*/*"
  ]
}
